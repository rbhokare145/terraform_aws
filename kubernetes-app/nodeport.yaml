---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: nodejs-nodeport
  name: nodejs-nodeport
spec:
  ports:
    -
      name: http
      port: 3000
      protocal: TCP
      targetPort: http
  selector:
    app: nodejs-sample
  type: NodePort


