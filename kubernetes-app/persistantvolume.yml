apiVersion: v1
kind : PersistentVolume
metadata:
  name : influxdb-pv
spec:
  capacity :
    storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain