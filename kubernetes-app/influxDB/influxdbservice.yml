apiVersion: v1
kind : Service
metadata:
  name: influxdb-service
  lables:
    app: influxdb

spec:
  selector:
    app: influxdb
  type: clusterIP
  ports:
    - name : http
      protocol: TCP
      targetPort: 8086
      port : 8086


