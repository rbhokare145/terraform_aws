---
apiVersion : v1
kind : Service
metadata:
  name : nodejs-lb
  labels :
    app : nodejs-sample
spec :
  selector :
    app : nodejs-sample
  type : LoadBalancer
  ports :
    - name : http
      protocol : TCP
      port : 3000
      targePort : 3000

